# CLAUDE.md

## Repository Overview

This is the **GitHub profile repository** for [@Rohitchitradurga](https://github.com/Rohitchitradurga). GitHub treats a repository named identically to the account username as a special profile repository — its `README.md` is rendered directly on the user's public GitHub profile page.

**Repository:** `rohitchitradurga/rohitchitradurga`  
**Purpose:** Personal profile/portfolio page displayed at github.com/Rohitchitradurga  
**Primary file:** `README.md`

---

## Repository Structure

```
rohitchitradurga/
└── README.md   # The profile page content (only file)
```

There are no source code files, build systems, tests, CI/CD pipelines, or dependency files in this repository. All content is Markdown.

---

## README.md Content Summary

The README presents Rohit's professional profile:

- **Identity:** Senior Cloud & Platform Engineer, ~10 years experience, AWS-focused
- **Core skills:** AWS, Terraform, CloudFormation, GitHub Actions, Jenkins, Docker, ECS, Kubernetes, Python, Bash, ELK stack, IAM/security
- **Featured projects:**
  1. AWS Cost Optimisation & FinOps Insight Platform (MVP) — no repo link yet
  2. Terraform AWS Enterprise Boilerplate — linked to `Rohitchitradurga/terraform-aws-enterprise-boilerplate`
  3. AWS Data Pipeline: JSON → Parquet → Analytics — linked to `Rohitchitradurga/aws-json-parquet-analytics-pipeline`
  4. CI/CD Pipelines Collection — no repo link yet
- **Contact:** LinkedIn at `www.linkedin.com/in/rohitchitradurga`

---

## Development Conventions

### Editing the README

- All content is GitHub-flavoured Markdown (GFM)
- The file uses HTML tags (`<h1>`, `<h3>`, `<p align="center">`) for alignment — preserve this style
- Emoji are used intentionally throughout for visual structure; maintain this convention
- Section headers follow the pattern: `## Section` with `---` horizontal rules as dividers
- Project entries use `### 🔸 Project Name` as sub-headers
- Repo links use the format: `👉 https://github.com/Rohitchitradurga/<repo-name>`
- Placeholder links use: `👉 *(Repository link coming soon)*`

### Commit Style

Commits in this repo are short and descriptive, focused on what changed:
- `"Update LinkedIn link in README"`
- `"Revamp README with detailed profile and projects"`

Follow the same pattern: imperative, concise, no ticket references needed.

### Branch Strategy

- `main` — production branch, what GitHub renders on the profile
- Feature branches for changes (e.g. `claude/add-claude-documentation-XpWXJ`)
- Merge to `main` when changes are ready to be visible on the profile

---

## What AI Assistants Should Know

1. **No code to run or test** — there are no scripts, no build steps, no linters. Changes are purely Markdown edits.
2. **Audience is public** — this is a public-facing professional profile. Keep tone professional and accurate.
3. **Only update factual content when instructed** — do not infer or fabricate project details, years of experience, or skill levels.
4. **Preserve formatting conventions** — the HTML/Markdown hybrid layout and emoji usage are intentional design choices.
5. **Repository links** — only add real, verified GitHub repo URLs. Use the `*(Repository link coming soon)*` placeholder for unlinked projects.
6. **No CLAUDE.md changes needed on routine edits** — update this file only when the repository structure, conventions, or workflow meaningfully changes.
